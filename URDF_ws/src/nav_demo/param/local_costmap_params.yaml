local_costmap:
  global_frame: odom #odometry global frame
  robot_base_frame: base_footprint #robot base frame
  update_frequency: 10.0 
  publish_frequency: 10.0 
  transform_tolerance: 0.5 
  inflation_radius: 0.3
  cost_scaling_factor: 3.0
  static_map: false  #need static map or not
  rolling_window: true #use rolling window or not
  width: 3 # local cost map width unit m
  height: 3 # local cost map height unit m
  resolution: 0.05 # local cost map resolution unit m，in general same with static map
